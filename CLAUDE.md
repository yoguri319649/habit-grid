# CLAUDE.md

このファイルは、このリポジトリでコードを扱う際に Claude Code (claude.ai/code) へ向けたガイダンスを提供します。

@AGENTS.md

## コマンド

- `npm run start`（または `npx expo start`）— Metro 開発サーバーを起動する
- `npm run ios` / `npm run android` / `npm run web` — 特定のプラットフォーム向けに開発サーバーを起動する
- `npm run lint` — `expo lint` を実行する（初回実行時に ESLint 設定のセットアップを提案される。設定ファイルはまだコミットされていない）
- `npm run reset-project` — 現在の `src/app` のスターターコードを `app-example/` に移動し、空の `src/app` を作成する。ユーザーが明示的にスターターコードの削除を求めた場合のみ実行すること（破壊的操作。`scripts/reset-project.js` 参照）

テストランナーは設定されていません（Jest のセットアップも `package.json` 内のテストスクリプトもありません）。

## アーキテクチャ

これは Expo Router（v57）を使用したアプリで、`src/` ディレクトリ構成を採用しています — ルートはリポジトリ直下ではなく `src/app` にあります。パスエイリアス `@/*` → `src/*` および `@/assets/*` → `assets/*` が `tsconfig.json` で設定されています。

- **ルーティング**: `src/app/_layout.tsx` がルートレイアウトです。`useColorScheme()` により駆動される `ThemeProvider`（`expo-router` 提供）でアプリ全体をラップし、`AppTabs` をレンダリングします。タブ画面は `src/app/index.tsx`（Home）と `src/app/explore.tsx`（Explore）— `app.json` で `typedRoutes` を有効にした `expo-router` によるファイルベースルーティングです。
- **タブバー**: `src/components/app-tabs.tsx` は `expo-router/unstable-native-tabs`（ネイティブのプラットフォームタブバー）を使用し、Web 向けには別実装の `app-tabs.web.tsx` があります。Expo Router は Web ビルド時に `.web.tsx` ファイルを自動的に解決します — このプラットフォーム分割パターン（`foo.tsx` + `foo.web.tsx`）は他の箇所でも使われています（例: `animated-icon.tsx` / `animated-icon.web.tsx`、`use-color-scheme.ts` / `use-color-scheme.web.ts`）。
- **テーマ**: `src/constants/theme.ts` で `Colors.light` / `Colors.dark`、`Fonts`（`Platform.select` によるプラットフォーム別フォントファミリー）、および余白・レイアウト用の定数（`Spacing`、`BottomTabInset`、`MaxContentWidth`）を定義しています。`src/hooks/use-theme.ts` は現在のカラースキームを `Colors` のエントリに解決します（`useColorScheme()` は `'unspecified'` を返すことがあり、これは `'light'` として扱われます）。コンポーネントはこれを `ThemedText` / `ThemedView`（`src/components/themed-text.tsx`、`themed-view.tsx`）経由で利用します。これらはスタイルバリアント用の `type` prop と、生の色や StyleSheet の色値ではなく `ThemeColor`（`Colors.light`/`Colors.dark` のキー）で指定するオプションの `themeColor` prop を受け取ります。
- **グローバル CSS**: `src/global.css` は `src/constants/theme.ts` からインポートされ、Web 版の `Fonts` の値が参照する CSS カスタムプロパティ（例: `--font-display`）を定義しています。

現時点でこのコードベースは未改変の Expo テンプレートスキャフォールド（`create-expo-app`）のままであり、habit-grid 固有の機能はまだ実装されていません。

## タスク管理アプリ 要件定義書(MVP)

### 1. 概要

#### 1.1 背景
既存のタスク管理アプリ(Todoist、Things、標準リマインダー等)には、複数のサブタスクを持つタスクにおいて、以下の課題がある。

- 各サブタスクが全体に占める重要度(ウェイト)や、個別の進捗を記録できない
- あるタスクにどのくらいの頻度・時間で取り組んでいるかが可視化されない

例として、「引っ越し」というタスクは「①不動産屋に連絡する」「②荷造り・部屋の片付けをする」「③会社や役所の手続きをする」のような性質の異なるサブタスクに分解されるが、既存アプリではこれらを均等な「チェック項目」としてしか扱えず、進捗の実態を反映できない。

#### 1.2 目的
サブタスクの重み付き進捗管理と、GitHubのコントリビューショングラフのような取り組み時間の可視化を組み合わせた、シンプルなタスク管理アプリを開発する。まずは自分専用に最適化した使い勝手を追求しつつ、一般ユーザーにも使いやすいUXを目指し、iOSアプリとして一般公開する。

#### 1.3 ターゲットユーザー
個人向け(一般公開)。特定の業界やチーム利用は想定しない。

#### 1.4 重視する体験
シンプルさ・ミニマルさ。凝った機能(重み調整、進捗の手動入力等)はデフォルトで意識しなくても成立するようにし、こだわりたいユーザーだけが深く使い込める設計とする。

---

### 2. コンセプト・差別化ポイント

1. **サブタスクの重み付き進捗管理**
   タスクを複数のサブタスクに分解し、各サブタスクに「全体への重み(%)」を持たせる。全体進捗は、各サブタスクの進捗とその重みから加重平均で自動計算する。
2. **自己申告ベースのコントリビューショングラフ**
   日々の取り組み時間を自己申告(ワンタップの時間チップ)で記録し、GitHubのコントリビューショングラフのように濃淡で可視化する。「頻度」ではなく「時間の積み重ね」を見える化する。

---

### 3. コア体験(コアループ)

1. タスクを作成する
2. 必要に応じてサブタスクに分割する(重みはデフォルトで均等割り、手動調整も可能)
3. サブタスクに取り組んだら、進捗スライダーを更新する(任意)
4. その日どのくらい取り組んだかを時間チップ(15分/30分/1時間/それ以上)でワンタップ入力する(任意)
5. 全体進捗は自動計算され、コントリビューショングラフにその日の記録が反映される

---

### 4. 画面構成

#### 4.1 画面一覧

| # | 画面名 | 概要 |
|---|--------|------|
| 1 | タスク一覧 | 保有する全タスクをタイトルと進捗バーで一覧表示 |
| 2 | タスク詳細 | サブタスクの進捗入力・時間チップ入力・コントリビューショングラフを1画面に統合 |
| 3 | タスク作成/編集 | タスク・サブタスクの新規作成、編集 |

#### 4.2 各画面の詳細仕様

##### タスク一覧画面
- 表示項目: タスクタイトル、全体進捗(パーセンテージ＋進捗バー)
- 新規タスク作成への導線(＋ボタン)
- タスクタップでタスク詳細画面へ遷移

##### タスク詳細画面
- ヘッダー: タスクタイトル、全体進捗(パーセンテージ＋進捗バー)
- サブタスク一覧: 各サブタスクの名称、重み(%)、進捗スライダー
- サブタスク追加ボタン
- 時間チップ入力: 「今日どのくらい進めた?」に対し、15分/30分/1時間/それ以上からワンタップで選択
- コントリビューショングラフ: そのタスクにおける直近の取り組み時間を日ごとの濃淡で表示

##### タスク作成/編集画面
- タスクタイトルの入力
- サブタスクの追加・削除・並び替え
- サブタスクの重み調整(デフォルト均等割り、手動で数値変更も可能)

---

### 5. MVP機能一覧

#### 5.1 MVPに含む機能(必須)

- タスクの作成・編集・削除
- サブタスクの追加・編集・削除
- サブタスクの重み設定(デフォルト均等割り、手動調整可能)
- サブタスクごとの進捗入力(手動スライダー)
- 全体進捗の自動計算(重み×各サブタスク進捗の加重平均)
- タスク一覧画面
- タスク詳細画面(サブタスク進捗＋時間チップ入力＋コントリビューショングラフ統合)
- 取り組み時間の自己申告記録(時間チップ:15分/30分/1時間/それ以上)
- コントリビューショングラフ表示(タスク単位、自己申告時間の合計を日別の濃淡で表示)
- タスクの完了操作

#### 5.2 MVP後に検討する機能(スコープ外)

- 全タスク横断のグラフ(1日の合計取り組み時間、全体の振り返り)
- 通知・リマインダー機能
- ウィジェット対応
- 他者との共有・共同作業機能
- タイマーによる自動計測
- 週次/月次のサマリー・統計分析
- カテゴリ・タグ・フィルタ・検索機能
- テーマ/ダークモード等の見た目カスタマイズ
- 複数端末間の同期(iCloud同期等)
- 初回起動時のオンボーディング説明

---

### 6. データモデル(概略)

| エンティティ | 主な項目 |
|---|---|
| Task(タスク) | id, タイトル, 作成日, 完了状態 |
| SubTask(サブタスク) | id, タスクID, 名称, 重み(%), 進捗(%), 並び順 |
| TimeLog(時間記録) | id, タスクID, 日付, 自己申告時間(分) |

---

### 7. 技術スタック

| 領域 | 選定技術 |
|---|---|
| 言語 | TypeScript |
| フレームワーク | Expo(Managed Workflow) + Expo Router |
| データ永続化 | expo-sqlite + Drizzle(ORM) |
| スライダーUI | @react-native-community/slider |
| コントリビューショングラフ | react-native-svg による自前描画 |
| スタイリング | NativeWind(Tailwind) |

---

### 8. 非機能要件(MVP時点の方針)

- 対応OS: iOSのみ(MVP時点)
- データ保存先: 端末内ローカル(expo-sqlite)。複数端末間の同期はMVP対象外
- パフォーマンス: 個人利用規模のデータ量(数十〜数百タスク程度)を前提とし、特別な最適化は行わない

---

### 9. 今後の検討事項

- サブタスクが多い場合の画面レイアウト(スクロール長大化への対応:アコーディオン化等)
- 時間チップ入力を促すタイミング(画面を開くたびに促すか、能動的な操作時のみ表示するか)
- 将来的な複数端末同期(iCloud等)の要否とタイミング
